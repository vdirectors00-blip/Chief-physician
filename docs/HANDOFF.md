# 비즈밸류 기업성장연구소 홈페이지 — 인수인계

작성 2026-08-01 (딥사이클 4회차까지 반영). 새 세션에서 이 파일을 먼저 읽으세요.

## 기본 정보

- 작업 폴더: `바탕 화면\프로젝트\경영주치의 홈페이지\site`
- 리포지토리: `vdirectors00-blip/Chief-physician` (push 하면 GitHub Pages 자동 배포, 반영까지 1~2분)
- 라이브: https://vdirectors00-blip.github.io/Chief-physician/
- 클라이언트: 정우성 대표 / 비즈밸류 기업성장연구소
- 시안 원본: `site/경영주치의.jpg` (1920 x 12200, 직원 디자인)

## 확정된 규칙 (임의로 뒤집지 말 것)

0. **정렬**: 사진을 깐 선언 구간(히어로 · 선언 배너 · 최종 CTA)만 가운데 정렬.
   나머지 콘텐츠 섹션은 제목과 같은 왼쪽 기준선에서 모든 블록이 시작한다.
   블록에 `margin-left:auto` 나 `justify-content:center` 를 새로 넣지 말 것
   (2026-08-01 에 이것 때문에 고민 목록이 1920px 에서 205px 어긋나 있었음)

1. **상호는 "비즈밸류 기업성장연구소"**. "경영주치의"는 사용자가 지은 별칭이라 랜딩 본문에 2곳만 남김
   (컨설턴트 섹션 제목 "경영주치의 정우성", 선언 배너 "경영주치의의 목표는~")
2. **파랑 = 행동**(버튼·링크·활성), **골드 `--gold:#c8973a` = 서수·라벨·아이콘**. 이 역할 분리를 지킬 것
3. **시안은 기준선이지 상한이 아님**. 사용자가 "시안 넘어도 된다"고 승인함 (2026-08-01)
4. **인물 얼굴이 크게 나오는 스톡 금지**. 동남아 촬영이 많아 위화감이 남. 건물·도시·드론 항공 위주로
   (레퍼런스: bnihero.kr — 송도 항공과 건물 사진으로 채운 사이트)
5. **히어로 문구는 "함께 설계합니다"**. 시안의 "성장합니다"를 요청서 원문대로 교정한 것이라 되돌리지 말 것
6. 사진·카피 선택은 사용자 몫. 후보를 보여주고 고르게 할 것
7. 문구를 바꾸면 `python tools/make_font_subset.py` 를 반드시 다시 실행
   (안 하면 새로 넣은 글자만 시스템 글꼴로 나옴)
8. 카드 사진은 **문서 / 건물 / 산업 / 항공이 섞이도록** 유지. 같은 계열이 두 장 이상이면
   한 화면에서 겹쳐 보임 (2026-08-01 에 이것 때문에 세 장을 갈아끼움)

## 지금까지 한 것

- 히어로: 블루아워 서울 파노라마 (색보정으로 파랑 강조)
- 로고: 카톡으로 받은 이미지를 벡터로 트레이스해 `assets/img/logo.svg`. 헤더·푸터·파비콘 적용
- 밀도: 섹션 여백 280 → 190px (시안 실측 181px). 전체 길이 15,086 → 13,600px대
- 구조: 히어로 → 고민(네이비) → 접근방식+3대가치(흰) → 6대 서비스 → ... 시안 흐름으로 복원
- 6대 서비스 카드: 사진 헤더 + 골드 아이콘 칩이 경계에 걸치는 배치
- 사진: 선언 배너(실루엣 미팅), 신청 폼(서울 파노라마), 접근 방식(서류·차트 좌우 분할)
- 타이포: 카드 설명 16.3 → 20.4px, 프로세스 18 → 20.5px, 대표 소개 19 → 22px
- 폰트 자체 호스팅 + 사이트 사용 글자만 서브셋 (330KB → 105KB)
- 파비콘·OG·canonical·sitemap.xml, 폼 라벨 노출, 페이스북 버튼

전체 전송량 765KB → 630KB대.

## 2026-08-01 (2차) 에 한 것

- **카드 사진 3장 교체** (사용자가 후보 시트에서 직접 고름)
  - 정부지원사업 → 국회 의원회관 (한국 실제 건물). 처음 고른 "기둥·계단 관공서" 는
    카드에 올리니 3D 모델처럼 보여 비교본을 만들어 다시 확정한 것
  - 기업인증 → 도장·만년필·인증씰이 놓인 책상 (사용자가 "인증은 서류 느낌" 으로 지정)
  - 법인전환 → 임원 회의실
  - 결과 구성: 문서 1 / 건물 2 / 인증서 1 / 회의실 1 / 항공 1
- **서비스 상세 6페이지 상단에 배경 사진**. 랜딩 카드와 **같은 파일을 재사용**해 용량 증가 없음
  (`gen_pages.py` 의 `PHOTO` 맵). 상세 헤더 로고도 랜딩과 같은 형태로 통일
- **컨설팅 사례 섹션을 밝게 뒤집음** (`sec--soft`). 프로세스~신청 폼이 길게 어둡던 구간이 끊김
- **푸터 확장**: 컨설팅 분야 6개 링크 + 개인정보 처리방침. 사업자 정보 자리는 그대로 둠
- CSS 실수 두 개 수정
  - `.card__fig` / `.card__icon` 블록이 통째로 중복돼 있던 것 (보이는 결과는 그대로 두고 정리)
  - `.chip:hover` 가 두 번 선언돼 파랑이 이겨서, 확정 규칙(골드=라벨)과 반대로 칠해지던 것
- 3대 가치 카드 제목 높이를 맞춰 본문 시작선 정렬, 카드 문구 줄바꿈 정리
- 로고 이미지에 width/height 지정 (레이아웃 밀림 방지)

검증: PC·모바일 전체 스크롤 눈으로 확인 / 사례 탭 전환 / 상세페이지 모바일에서
빵부스러기가 헤더에 안 가림(좌표 대조) / 폼 전송 → 완료 화면(주소 붙였을 때) /
링크·앵커 전수 확인 / 콘솔 에러 없음.

## 2026-08-01 (3차) 에 한 것 — 정렬 체계

좌표로 재보니 섹션마다 왼쪽 기준선이 흔들리고 있었음. 위 0번 규칙을 세우고 정리.

- 고민 목록이 1010px 가운데 정렬이라 1920px 에서 제목보다 205px 안쪽에서 시작하던 것 수정
- 3대 가치 제목·흐름 칩, 협업 분야 칩, 핵심 포지셔닝 박스를 왼쪽 기준선으로
- 협업 분야 8칩을 컨테이너를 채우는 4열로 (오른쪽이 크게 비어 있었음)
- 서비스 카드 아이콘 칩: 반투명 골드 → 불투명 흰 바탕 + 골드 테두리
  (건물·회의실 같은 밝은 사진 위에서 아이콘이 묻혔음)
- 컨설턴트 키워드 5개를 다른 칩과 같은 알약 형태로
- 소제목을 골드로 (골드=라벨 규칙)

**정정**: 4단계(진단→전략→실행→성장관리)와 프로세스 STEP 01~05 는 중복이 아니라
요청서 5번·9번이 각각 지정한 필수 항목임. 합치거나 지우지 말 것. 시각적 무게만 낮췄음.

## 다음 할 일

- 대표님 실사진·사례 2건·사업자 정보·연락처가 들어오면 그 자리 채우기 (아래 참조)
- `form-city.webp` 가 138KB 로 이 사이트에서 제일 무거운 이미지.
  어두운 오버레이에 덮이는 배경이라 더 줄일 여지가 있는데,
  **이미 압축된 파일을 다시 압축하면 오히려 커짐** (2026-08-01 에 시도했다가 되돌림).
  줄이려면 원본을 다시 받아 작은 치수로 뽑아야 함
- 상세 6페이지가 공개자료 기반 일반론이라 경쟁사와 차별화가 약함.
  정우성 대표 인터뷰로 실제 판단 기준·사례를 받아야 풀림

### 클라이언트 자료를 받아야 하는 것 (제작으로 못 메움)

`클라이언트_요청자료_카톡용.txt` 참고.

- 대표님 실사진 — 지금은 얼굴 없는 스톡. 신뢰를 만드는 유일한 자리가 비어 있음
- 컨설팅 사례 2건 — CASE 02·03 이 "자료 확정 후 기재" 로 방문자에게 그대로 보임
- 사업자 정보 — 푸터와 개인정보 처리방침의 자리표시자
- 접수 이메일 / 전화 / 카카오 채널 / 페이스북 주소 — 지금은 눌러도 안내만 뜸
- 숫자 실적 — bnihero 의 6,406+ 같은 스트립을 넣으려면 필요. 지어낼 수 없음

## 지금 켜져 있는 임시 장치 (사진 확정되면 지울 것)

`index.html` 맨 아래 **메인 사진 고르기 바**(`.pickbar` 블록)와 `assets/img/_cand/` 폴더.
대표님이 초안에서 직접 히어로 사진을 고르시라고 넣은 것이다.

고르시면 ① 그 파일을 `hero.jpg` / `hero.webp` / `hero-4k.webp` 로 정식 변환해 넣고
② `.pickbar` 주석 블록(스타일·마크업·스크립트)과 `_cand/` 폴더를 지운다.
③ 밝은 사진을 택하면 `.hero__bg::after` 의 겹칠을 선택 바에 쓰던 옅은 값으로 바꿔야 한다.
지금 겹칠을 그대로 두면 밝은 사진도 다시 가라앉는다.

## 함정

- 공개 시 `robots.txt` 의 `Disallow: /` 와 각 페이지 head 의 `noindex` 를 함께 지워야 함
- 정식 도메인이 정해지면 canonical / og:url / og:image / sitemap.xml 의 주소를 전부 교체
- 배경 사진은 CSS background 라 지연 로딩이 안 됨. 새로 추가할 때 용량 확인할 것
- GitHub Pages 는 HTML 이 먼저 갱신되고 이미지가 늦게 붙는 경우가 있음. 쿼리스트링으로 캐시 우회해 확인
- **접수 주소를 붙이면 실제로 한 건 넣어보고 메일·시트까지 도착하는지 확인할 것.**
  지금 코드는 전송이 실패하면 "전송에 실패했습니다" 만 띄우고 그 신청은 사라짐.
  받는 쪽이 CORS 를 안 열어두면 전송이 실패로 처리됨 (Formspree·앱스스크립트는 정상)
- 화면을 스크린샷으로 확인할 때 **요소 단위로 찍으면 고정 헤더가 그 위에 겹쳐 찍힘**.
  카드 사진 비교본을 만들 때 헤더가 사진을 가려서 헤더를 숨기고 다시 찍었음
